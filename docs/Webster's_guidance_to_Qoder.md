# Webster's Guidance to Qoder

## Basics

- Please keep this file as guidance when you think (do the reasoning) or / and create any artifacts (including documents and code).

## Specifics

- After a change is made inside the local folder, you need to subsequently push the changes into GitHub, and then trigger the deployment inside CloudFlare based environment when there is a code change. This specific rule can not be changed unless I (Webster) give you exclusive order.
- After some steps of operations inside Quest window, whenever the context size reaches the 70% limit, please do the compression automatically.
- Starting from March 17, 2026, all the documents should be generated using Chinese (by default) unless there is a need for English usage. For existing English-based documents, we will need to convert them into the Chinese version, and this needs waiting for my further signal. 
